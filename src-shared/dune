(library
 (name gcloud_shared)
 (public_name gcloud.shared)
 (wrapped false))

(subdir
  melange
  (library
    (name gcloud_shared_melange)
    (public_name gcloud.shared-melange)
    (modes melange)
    (wrapped false))
 (copy_files# "../*.ml")
 (copy_files# "../*.re"))
